---
"previewhost": major
---

Use Previewhost names for runtime directories, Docker resources, the managed PostgreSQL user and database, and the gateway hop header.

Before updating, stop existing owners with the installed version. Earlier runtime directories and managed database records are not migrated. See the reset instructions in README.md.
