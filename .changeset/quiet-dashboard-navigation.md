---
"previewhost": minor
---

Rebuild the dashboard with React and shared UI components. Add stable navigation, searchable and wrapping logs, readable preview URLs, and bounded configuration and secret lists.
